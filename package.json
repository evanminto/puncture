{
  "name": "@evanminto/puncture",
  "version": "0.0.12",
  "description": "A minimal-setup, framework-agnostic pattern library tool implemented using Web Components.",
  "main": "dist/puncture.js",
  "browser": "dist/puncture.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "watch": "rollup -c --watch",
    "prepare": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^1.15.3",
    "rollup-plugin-commonjs": "^10.0.0",
    "rollup-plugin-copy-assets": "^2.0.1",
    "rollup-plugin-node-resolve": "^5.0.2",
    "rollup-plugin-terser": "^5.0.0"
  },
  "dependencies": {
    "lit-element": "^2.2.0",
    "pretty": "^2.0.0"
  }
}
